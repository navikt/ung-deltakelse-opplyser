query($ident: ID!, $grupper: [IdentGruppe!]) {
    hentIdenter(ident: $ident, grupper:$grupper, historikk: true) {
        identer {
            ident,
            historisk
            gruppe
        }
    }
}
